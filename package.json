{
  "name": "locations",
  "version": "1.0.0",
  "private": true,
  "description": "locations generated with Skaffolder Angular 4 - Node JS template",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "start": "concurrently --raw \"cd server && npm start\" \"cd client && npm run watch\" ",
    "build": "concurrently --raw \"cd server && npm run build\" \"cd client && npm run build\" ",
    "debug": "concurrently --raw \"cd server && npm run debug\" \"cd client && npm run build\" ",
    "lint": "tslint ./**/*.ts -t verbose"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.2.0",
    "copy-webpack-plugin": "^4.0.1"
  },
  "repository": {}
}
